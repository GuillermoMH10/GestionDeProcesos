const contactForm=document.getElementById("contactForm");
const contactStatus=document.getElementById("contactStatus");
function showError(field,message){const group=field.closest(".form-group");group.classList.add("invalid");group.querySelector(".field-error").textContent=message;}
function clearErrors(){contactForm.querySelectorAll(".form-group").forEach(group=>{group.classList.remove("invalid");group.querySelector(".field-error").textContent="";});contactStatus.textContent="";}
contactForm.addEventListener("submit",event=>{event.preventDefault();clearErrors();const name=document.getElementById("contactName");const email=document.getElementById("contactEmail");const subject=document.getElementById("contactSubject");const message=document.getElementById("contactMessage");let valid=true;if(name.value.trim().length<2){showError(name,"Escribe un nombre válido.");valid=false;}if(!email.validity.valid){showError(email,"Escribe un correo válido.");valid=false;}if(!subject.value){showError(subject,"Selecciona un asunto.");valid=false;}if(message.value.trim().length<10){showError(message,"El mensaje debe tener al menos 10 caracteres.");valid=false;}if(!valid)return;contactStatus.textContent="Mensaje enviado correctamente. Nos comunicaremos contigo pronto.";contactForm.reset();});
